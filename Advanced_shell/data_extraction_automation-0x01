#!/bin/bash

# Extract values using jq
name=$(jq -r '.name' data.json)
height=$(jq -r '.height' data.json)
weight=$(jq -r '.weight' data.json)
type=$(jq -r '.types[0].type.name' data.json)

# Format height (decimeters → meters)
formatted_height=$(awk "BEGIN { printf \"%.1f\", $height / 10 }")

# Format weight (hectograms → kg)
formatted_weight=$(awk "BEGIN { printf \"%.0f\", $weight / 10 }")

# Capitalize type (first letter uppercase)
type=$(echo "$type" | sed 's/.*/\u&/')

# Capitalize name (first letter uppercase)
name=$(echo "$name" | sed 's/.*/\u&/')

# FINAL OUTPUT (must match checker EXACTLY)
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
